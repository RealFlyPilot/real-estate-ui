import * as React from 'react';

  
  export interface CustomizeTypographyProps {
              
  }
  
  export const CustomizeTypography: React.SFC<CustomizeTypographyProps> = () => {
      return (
          <div>CustomizeTypography</div>
      );
  }